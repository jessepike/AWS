# Backlog: Agentic Work System

## Active Items

| ID | Item | Type | Pri | Why |
|----|------|------|-----|-----|
| B5 | Formalize ring instantiation — how each ring operates at each layer, current vs. target state | Architecture | P2 | Rings are defined theoretically but have no operational meaning yet |
| B6 | Cross-cutting agent teams (Validation, Review, Improvement) — design and build shared dispatch pattern | Architecture | P2 | Review/validation work is ad-hoc; duplicated effort per project without a shared pattern |
| B7 | Component integration testing — verify each component pair connects as designed | Quality | P2 | Components tested in isolation; no validation that pairs actually connect end-to-end |
| B8 | Operational runbooks — how to use the system day-to-day, common workflows, troubleshooting | Docs | P3 | Day-to-day use relies on memory, not documented workflows |
| B10 | Enterprise evolution notes — document what would need to change for multi-user/team scale | Architecture | P3 | Personal-scale decisions may be wrong at team scale; better to flag early than redesign later |
| B11 | Trust boundaries — map trust boundaries for each component and cross-component interactions. Where does trust start/stop? What can each component do autonomously vs. gated? Cascade findings to component-level backlogs where applicable. | Architecture | P1 | Agents can cause damage if trust is implicit; need explicit boundaries before expanding autonomy |
| B12 | System-to-component design cascade — establish pattern for capturing when system-level designs (e.g., trust boundaries) need implementation at subordinate component level. Prevent system-level decisions from staying abstract. | Governance | P1 | System-level decisions (trust, protocols) stay abstract and never get implemented downstream |
| B13 | Review Krypton autonomy gates — understand hook firing frequency (every tool use?), performance impact, necessity. Are PreToolUse/PostToolUse hooks firing on every action? What's the overhead? What does the redaction library do and is it needed? | Review | P1 | Hooks on every tool call may create unmeasured overhead; need to know the cost vs. value |
| B14 | Review data flow patterns 1 & 2 — Pattern 1 (capture → storage via Krypton routing): is this accurate? Link Triage is a primary capture path too. Pattern 2 (focus synthesis): validate the parallel gatherer model. Should there be a Work Mgmt gatherer? | Review | P1 | Architecture doc may not reflect reality; incorrect mental model leads to wrong design decisions |
| B15 | Review session protocol enforcement (pattern 4) — how are session start/end instructions actually enforced? Via hooks? CLAUDE.md conventions? What ensures compliance? | Review | P2 | Conventions only work if followed; unclear whether enforcement is real or aspirational |
| B16 | Deep dive on integration gaps — expand the 6 identified gaps into actionable items with scope, effort, and priority | Architecture | P2 | 6 gaps identified but not scoped; can't prioritize investment without knowing effort |
| B17 | Deep dive on MCP server tools — review each server's tool inventory. Work Mgmt 40+ tools seems high. Are they all needed? What's the right tool count? Review KB Manager plugin role vs. KB MCP server. | Review | P2 | 40+ WM tools is a signal of possible bloat or unclear responsibilities |
| B18 | Review model tier mapping in architecture.md — does intent layer truly require Opus minimum? What are we conveying? The gradient (heavier reasoning = higher layers) makes sense but minimum model requirements may be too prescriptive. | Architecture | P2 | Opus minimums at upper layers may create unnecessary cost without commensurate benefit |
| B19 | Clarify brief.md scope — articulate the meta-governing purpose: this project ensures alignment and connectivity across all components. Each component self-governs internally; this project governs the whole. How does it handle self-improvement? How does it handle cross-component alignment? | Architecture | P1 | Ambiguous meta-project purpose leads to scope creep, neglect, or duplicated governance |
| B21 | Architecture diagrams — create visual models at multiple levels: (1) macro system overview, (2) communication protocols / data flows, (3) trust boundaries, (4) component connections. Partially addressed by Nerve Center's desktop companion strategic mode (constellation, topology views). See `docs/nerve-center-architecture.md` §4.2. Static diagram generation still needed separately. | Docs | P1 | No visual model means auditing and onboarding require reading all docs serially |
| B22 | Documentation standard — define a standard documentation structure for every component, with two audiences: (a) user-level docs (layman, end-user) and (b) technical docs (builder, detailed). Design at AWS level, implement at each component level. Cascading item per B12 pattern. | Docs | P1 | Each component docs differently; inconsistency makes cross-component work and audits harder |
| B25 | Review: Try batch backlog with specific IDs pattern — specify backlog item IDs upfront in session prompts for higher goal achievement. Low effort to try, high potential impact. Insight: sessions with specific IDs → fully_achieved; vague starts → partially_achieved. | Workflow | P2 | Sessions with explicit IDs achieve more; cheap workflow change with measurable upside |
| B26 | Review: Try task agents for parallel review/audit workflows — spawn sub-agents for review cycles while main session continues implementation. Already have well-defined review processes that could delegate. | Workflow | P3 | Review cycles could run in parallel; reduces context switching and session length |
| B27 | Review: Evaluate parallel backlog execution feasibility — spawn parallel Claude Code agents per backlog item with coordinator for conflict resolution. Horizon capability requiring reliable multi-agent coordination. | Architecture | P3 | Sequential execution is the bottleneck for ambitious sessions |
| B28 | Review: Evaluate self-healing CI loop — autonomous test-fix-retest loop with max iterations. Would eliminate babysitting implementation sessions. | Automation | P3 | Buggy code sessions require constant oversight; autonomous loops would free attention for higher-value work |
| B29 | Review: Evaluate autonomous lifecycle orchestrator — orchestrator reads ADF stage defs, dispatches sub-agents per phase, escalates for gate approvals only. End-state vision for the agentic work system. | Architecture | P3 | End-state: human only approves gates; everything else runs autonomously |
| B30 | Cascade Why/Realized backlog fields to all component projects — update each component's BACKLOG.md to match ADF spec v1.1.0 (add Why to queue, Realized? to archive). Components: ADF, KB, Memory, Capabilities Registry, Krypton, Link Triage, Work Management, Nerve Center. WM also needs schema migration (add `intent` + `outcome` columns to backlog_item table and MCP API). | Governance | P1 | Done |

## Completed

| ID | Item | Date | Realized? |
|----|------|------|-----------|
| B1 | Inter-component communication protocols — `docs/communication-protocols.md` | 2026-02-11 | Partial — doc exists but protocols not enforced in practice; components still integrate ad-hoc |
| B2 | Governance layer — superseded by nerve-center monitoring jobs: governance-sentinel, drift-detector, priority-alignment, backlog-health, session-discipline all running in production. | 2026-02-23 | Yes — 5 monitoring jobs running in production, daily/weekly automated governance |
| B4 | Cross-project observability / operations center — nerve-center project. Phase 4 (desktop) complete, iOS + Slack + Railway all shipped. | 2026-02-23 | Yes — fully delivered, actively used daily across iOS + Slack + Desktop |
| B9 | Autonomy escalation tracking — nerve-center autonomy-assessor job (weekly) + desktop companion strategic mode (Phase 4 complete). | 2026-02-23 | Yes — weekly automated assessment running; strategic mode in desktop companion |
| B23 | SDK automation spike (Drift Detector) — superseded by nerve-center project. `claude -p` pattern validated, 7 monitoring jobs in production. | 2026-02-23 | Yes — pattern validated and scaled to 30+ jobs; exceeded original scope |
| B24 | CC Insights CLAUDE.md conventions — added 3 to global CLAUDE.md: multi-agent stale overwrite, YAML append-only, ADF lifecycle enforcement. | 2026-02-23 | Partial — conventions added but not yet validated across sessions; check in 2 weeks |
| B3 | Krypton `/capture` + `/ingest` commands route to KB, Memory, WM backlog. WM gatherer, Slack reporting, capture envelope standard all shipped. | 2026-02-24 | Yes — routing architecture delivered; automated capture gap (separate Krypton item) still open |
| B20 | ADF fifth stage — routed to ADF project backlog as B87. Operate & Learn spec drafted with loop closure mechanics and lifecycle diagrams. | 2026-02-24 |
| B30 | Cascade Why/Realized fields to all 8 component BACKLOG.md files. ADF spec updated to v1.1.0. WM schema migration tracked separately (WM backlog item aee4013c). | 2026-02-24 | Yes — spec drafted, routed correctly; value realized when ADF project closes B87 |
